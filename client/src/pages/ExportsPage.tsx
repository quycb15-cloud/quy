import { EmptyState, PageHeader, Panel } from "@/components/PageHeader";
import { QueryError } from "@/components/QueryError";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { DEFAULT_PERIOD, STANDARD_PERIODS, formatDate, formatQuantity, toDateInput } from "@/lib/rubber";
import { calculateProjectedWarehouseLoss } from "@/lib/exportWarehouseLoss";
import { filterTeamExportJournal, toTeamExportExcelRows, type TeamExportJournalRow } from "@/lib/exportJournal";
import { trpc } from "@/lib/trpc";
import { comparePeriodLabel, compareTeamName } from "@shared/teamOrder";
import { Calculator, Download, Pencil, RotateCcw, Save, Trash2, X } from "lucide-react";
import { FormEvent, useMemo, useState } from "react";
import { toast } from "sonner";

export default function ExportsPage() {
  const utils = trpc.useUtils();
  const { data: plots, error: plotsError } = trpc.rubber.plots.list.useQuery();
  const { data: records, isLoading, error: recordsError } = trpc.rubber.exports.teamList.useQuery();
  const [unit, setUnit] = useState("");
  const [periodLabel, setPeriodLabel] = useState<string>(DEFAULT_PERIOD);
  const [recordDate, setRecordDate] = useState(toDateInput());
  const [frozenContaminatedLatex, setFrozenContaminatedLatex] = useState("");
  const [latexThread, setLatexThread] = useState("");
  const [note, setNote] = useState("");
  const [fromDate, setFromDate] = useState("");
  const [toDate, setToDate] = useState("");
  const [journalPeriod, setJournalPeriod] = useState("");
  const [editingId, setEditingId] = useState<number | null>(null);
  const totalExport = useMemo(() => Number(frozenContaminatedLatex || 0) + Number(latexThread || 0), [frozenContaminatedLatex, latexThread]);
  const units = useMemo(() => Array.from(new Set((plots ?? []).map(plot => plot.unit))).sort(compareTeamName), [plots]);
  const { data: balance } = trpc.rubber.exports.teamBalance.useQuery({ unit, periodLabel }, { enabled: Boolean(unit) });
  const projectedLoss = calculateProjectedWarehouseLoss(balance?.totalImport ?? 0, balance?.totalExport ?? 0, totalExport);
  const journalPeriods = useMemo(() => Array.from(new Set((records ?? []).map(row => row.periodLabel))).sort(comparePeriodLabel), [records]);
  const filteredRecords = useMemo(() => [...filterTeamExportJournal(records ?? [], { unit, periodLabel: journalPeriod, fromDate, toDate })].sort((left, right) => new Date(right.recordDate).getTime() - new Date(left.recordDate).getTime() || comparePeriodLabel(left.periodLabel, right.periodLabel) || compareTeamName(left.unit, right.unit)), [records, unit, journalPeriod, fromDate, toDate]);

  const refresh = async () => Promise.all([utils.rubber.exports.teamList.invalidate(), utils.rubber.exports.teamBalance.invalidate(), utils.rubber.dashboard.invalidate(), utils.rubber.periods.invalidate()]);
  const clearEditing = () => { setEditingId(null); setFrozenContaminatedLatex(""); setLatexThread(""); setNote(""); };
  const create = trpc.rubber.exports.teamCreate.useMutation({ onSuccess: async () => { await refresh(); toast.success("Đã lưu xuất mủ theo Đội"); clearEditing(); }, onError: e => toast.error(e.message) });
  const update = trpc.rubber.exports.teamUpdate.useMutation({ onSuccess: async () => { await refresh(); toast.success("Đã cập nhật xuất mủ theo Đội"); clearEditing(); }, onError: e => toast.error(e.message) });
  const remove = trpc.rubber.exports.teamRemove.useMutation({ onSuccess: async () => { await refresh(); toast.success("Đã xóa bản ghi xuất mủ theo Đội"); clearEditing(); }, onError: e => toast.error(e.message) });
  const isMutating = create.isPending || update.isPending || remove.isPending;

  const submit = (event: FormEvent) => {
    event.preventDefault();
    const payload = { unit, periodLabel, recordDate: new Date(`${recordDate}T12:00:00`), frozenContaminatedLatex: Number(frozenContaminatedLatex), latexThread: Number(latexThread), note };
    if (editingId) update.mutate({ id: editingId, data: payload });
    else create.mutate(payload);
  };
  const loadRecord = (row: TeamExportJournalRow) => { setEditingId(row.id); setUnit(row.unit); setPeriodLabel(row.periodLabel); setRecordDate(new Date(row.recordDate).toISOString().slice(0, 10)); setFrozenContaminatedLatex(String(row.frozenContaminatedLatex)); setLatexThread(String(row.latexThread)); setNote(row.note ?? ""); };
  const cancelEditing = () => { setEditingId(null); setFrozenContaminatedLatex(""); setLatexThread(""); setNote(""); };
  const confirmRemove = () => { if (editingId && window.confirm("Bạn có chắc muốn xóa bản ghi xuất mủ này không?")) remove.mutate({ id: editingId }); };
  const exportJournal = async () => {
    if (!unit) return toast.error("Chọn Đội trước khi xuất Excel");
    if (!filteredRecords.length) return toast.error("Không có dữ liệu Xuất mủ trong khoảng đã chọn");
    const XLSX = await import("xlsx");
    const workbook = XLSX.utils.book_new();
    XLSX.utils.book_append_sheet(workbook, XLSX.utils.json_to_sheet(toTeamExportExcelRows(filteredRecords)), "Nhật ký Xuất mủ");
    XLSX.writeFile(workbook, `nhat-ky-xuat-mu-${unit.replace(/\s+/g, "-")}-${journalPeriod.replace(/\s+/g, "-") || "tat-ca-dot"}-${fromDate || "tu-dau"}-${toDate || "den-nay"}.xlsx`);
    toast.success(`Đã xuất ${filteredRecords.length} dòng Xuất mủ của ${unit}`);
  };

  return <div className="page-enter"><PageHeader eyebrow="Xuất kho theo kỳ" title="Xuất mủ" description="Ghi nhận mủ đông tạp và mủ dây xuất kho. Cộng xuất được tự động tính để liên kết với báo cáo tiến độ." />
    {plotsError || recordsError ? <div className="mb-5"><QueryError message={plotsError?.message || recordsError?.message} /></div> : null}
    <div className="grid min-w-0 gap-5 xl:grid-cols-[0.92fr_1.4fr]">
      <Panel title={editingId ? "Sửa ghi nhận xuất mủ" : "Ghi nhận xuất mủ"} description="Bản ghi xuất mủ được tổng hợp theo Đội, ngày và đợt vận hành.">
        {!plots?.length ? <EmptyState title="Cần khai báo vườn trước" description="Hãy yêu cầu quản trị viên thêm vườn rồi quay lại ghi nhận xuất mủ." /> : <form onSubmit={submit} className="grid gap-4"><Field label="Đội"><select value={unit} onChange={e => setUnit(e.target.value)} required className="h-11 w-full min-w-0 rounded-md border border-input bg-background px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring"><option value="">Chọn đội</option>{units.map(item => <option key={item} value={item}>{item}</option>)}</select></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Ngày xuất"><Input className="h-11 min-w-0" type="date" value={recordDate} onChange={e => setRecordDate(e.target.value)} required /></Field><Field label="Đợt"><select value={periodLabel} onChange={e => setPeriodLabel(e.target.value)} required className="h-11 w-full min-w-0 rounded-md border border-input bg-background px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring">{STANDARD_PERIODS.map(period => <option key={period} value={period}>{period}</option>)}</select></Field></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Mủ đông tạp (kg)"><Input className="h-11 min-w-0" type="number" min="0" step="0.01" value={frozenContaminatedLatex} onChange={e => setFrozenContaminatedLatex(e.target.value)} placeholder="0.00" required /></Field><Field label="Mủ dây (kg)"><Input className="h-11 min-w-0" type="number" min="0" step="0.01" value={latexThread} onChange={e => setLatexThread(e.target.value)} placeholder="0.00" required /></Field></div><div className="grid gap-3 rounded-xl border border-amber-100 bg-amber-50 p-4"><div className="flex items-center gap-2 text-sm font-semibold text-amber-800"><Calculator className="h-4 w-4" />Hao kho dự kiến</div>{!unit ? <p className="text-sm text-amber-700">Chọn Đội để tính hao kho theo số liệu nhập – xuất thực tế.</p> : <><div className="grid grid-cols-1 gap-3 text-sm min-[360px]:grid-cols-2"><p>Nhập cùng Đợt: <strong>{formatQuantity(balance?.totalImport ?? 0)} kg</strong></p><p>Đã xuất: <strong>{formatQuantity(balance?.totalExport ?? 0)} kg</strong></p></div><p className="font-display text-3xl font-bold text-amber-900">{formatQuantity(projectedLoss.lossKg)} <span className="text-sm">kg</span></p><p className="text-xs text-amber-700/80">Nhập cùng Đợt − cộng xuất đã ghi − bản xuất đang nhập{projectedLoss.exceedsImportKg > 0 ? ` · Xuất vượt nhập ${formatQuantity(projectedLoss.exceedsImportKg)} kg` : ""}</p></>} </div><div className="rounded-xl border border-sky-100 bg-sky-50 p-4"><div className="flex items-center gap-2 text-sm font-semibold text-sky-800"><Calculator className="h-4 w-4" />Cộng xuất tự tính</div><p className="font-display mt-2 text-3xl font-bold text-sky-900">{formatQuantity(totalExport)} <span className="text-sm">kg</span></p><p className="mt-1 text-xs text-sky-700/80">Mủ đông tạp + mủ dây</p></div><Field label="Ghi chú"><Textarea className="min-h-[88px] w-full min-w-0 break-words" value={note} onChange={e => setNote(e.target.value)} placeholder="Ghi chú cho bản ghi (nếu có)" /></Field><div className="flex flex-col gap-2 sm:flex-row sm:flex-wrap"><Button disabled={isMutating} className="mt-1 min-h-11 w-full bg-emerald-700 hover:bg-emerald-800 sm:w-auto">{isMutating ? "Đang xử lý…" : editingId ? <><Save className="mr-2 h-4 w-4" />Cập nhật xuất mủ</> : <><Save className="mr-2 h-4 w-4" />Lưu xuất mủ</>}</Button>{editingId ? <><Button type="button" variant="outline" className="min-h-11 w-full border-red-200 text-red-700 hover:bg-red-50 sm:w-auto" onClick={confirmRemove} disabled={isMutating}><Trash2 className="mr-2 h-4 w-4" />Xóa bản ghi</Button><Button type="button" variant="outline" className="min-h-11 w-full sm:w-auto" onClick={cancelEditing}><X className="mr-2 h-4 w-4" />Hủy sửa</Button></> : null}</div></form>}
      </Panel>
      <Panel title="Nhật ký xuất mủ" description="Tra cứu và xuất báo cáo theo Đội, Đợt, khoảng thời gian; có thể sửa hoặc xóa từng bản ghi theo quyền được cấp."><div className="mb-4 grid gap-3 rounded-xl border border-slate-100 bg-slate-50 p-3 sm:grid-cols-3 xl:grid-cols-5"><Field label="Đợt"><select value={journalPeriod} onChange={event => setJournalPeriod(event.target.value)} className="h-11 w-full min-w-0 rounded-md border border-input bg-background px-3 text-sm outline-none focus-visible:ring-2 focus-visible:ring-ring"><option value="">Tất cả Đợt</option>{journalPeriods.map(period => <option key={period} value={period}>{period}</option>)}</select></Field><Field label="Từ ngày"><Input className="h-11 min-w-0" type="date" value={fromDate} max={toDate || undefined} onChange={event => setFromDate(event.target.value)} /></Field><Field label="Đến ngày"><Input className="h-11 min-w-0" type="date" value={toDate} min={fromDate || undefined} onChange={event => setToDate(event.target.value)} /></Field><div className="flex items-end"><Button type="button" variant="outline" className="min-h-11 w-full" onClick={() => { setJournalPeriod(""); setFromDate(""); setToDate(""); }}><RotateCcw className="mr-2 h-4 w-4" />Đặt lại</Button></div><div className="flex items-end"><Button type="button" className="min-h-11 w-full bg-emerald-700 hover:bg-emerald-800" onClick={exportJournal}><Download className="mr-2 h-4 w-4" />Xuất Excel</Button></div></div>{isLoading ? <div className="h-56 animate-pulse rounded-xl bg-slate-100" /> : !filteredRecords.length ? <EmptyState title="Chưa có bản ghi phù hợp" description={unit || journalPeriod || fromDate || toDate ? "Hãy thay đổi Đội, Đợt hoặc khoảng ngày để tra cứu." : "Dữ liệu xuất mủ theo kỳ sẽ xuất hiện tại đây."} /> : <><div className="space-y-3 md:hidden">{filteredRecords.map(row => <article key={`mobile-${row.id}`} className="min-w-0 rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex min-w-0 items-start justify-between gap-3"><div className="min-w-0"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{formatDate(row.recordDate)} · {row.periodLabel}</p><p className="mt-1 break-words font-semibold text-slate-800">{row.unit}</p><p className="mt-1 break-words text-sm text-slate-500">Người lập: {row.preparedBy || "—"}</p></div><p className="shrink-0 text-right font-bold text-sky-700">{formatQuantity(row.totalExport)} kg</p></div><dl className="mt-3 grid grid-cols-2 gap-3 border-t border-slate-100 pt-3 text-sm"><div className="min-w-0"><dt className="text-xs text-slate-400">Mủ đông tạp</dt><dd className="break-words font-medium text-slate-700">{formatQuantity(row.frozenContaminatedLatex)} kg</dd></div><div className="min-w-0"><dt className="text-xs text-slate-400">Mủ dây</dt><dd className="break-words font-medium text-slate-700">{formatQuantity(row.latexThread)} kg</dd></div></dl><p className="mt-3 break-words text-sm text-slate-600"><span className="font-medium text-slate-500">Ghi chú:</span> {row.note || "—"}</p><Button type="button" variant="outline" className="mt-3 min-h-11 w-full" onClick={() => loadRecord(row)}><Pencil className="mr-1 h-4 w-4" />Sửa</Button></article>)}</div><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[920px] text-left"><thead><tr className="border-b border-slate-200 text-xs font-bold uppercase tracking-[0.1em] text-slate-400"><th className="px-3 py-3">Ngày</th><th className="px-3 py-3">Đội</th><th className="px-3 py-3">Đợt</th><th className="px-3 py-3 text-right">Mủ đông tạp</th><th className="px-3 py-3 text-right">Mủ dây</th><th className="px-3 py-3 text-right">Cộng xuất</th><th className="px-3 py-3">Người lập</th><th className="px-3 py-3">Ghi chú</th><th className="px-3 py-3">Thao tác</th></tr></thead><tbody>{filteredRecords.map(row => <tr key={row.id} className="border-b border-slate-100 last:border-0"><td className="px-3 py-4 text-sm text-slate-600">{formatDate(row.recordDate)}</td><td className="px-3 py-4 break-words font-semibold text-slate-800">{row.unit}</td><td className="px-3 py-4 text-sm text-slate-600">{row.periodLabel}</td><td className="px-3 py-4 text-right text-sm">{formatQuantity(row.frozenContaminatedLatex)}</td><td className="px-3 py-4 text-right text-sm">{formatQuantity(row.latexThread)}</td><td className="px-3 py-4 text-right font-bold text-sky-700">{formatQuantity(row.totalExport)} kg</td><td className="px-3 py-4 break-words text-sm text-slate-600">{row.preparedBy || "—"}</td><td className="max-w-48 break-words px-3 py-4 text-sm text-slate-600">{row.note || "—"}</td><td className="px-3 py-4"><Button type="button" variant="outline" className="min-h-11" size="sm" onClick={() => loadRecord(row)}><Pencil className="mr-1 h-3 w-3" />Sửa</Button></td></tr>)}</tbody></table></div></>}</Panel>
    </div>
  </div>;
}

function Field({ label, children }: { label: string; children: React.ReactNode }) { return <div className="grid gap-2"><Label className="text-sm font-semibold text-slate-700">{label}</Label>{children}</div>; }
